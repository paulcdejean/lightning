# Handoff — iteration 1 (AWS CLI installed)

## What this iteration did
- Added `awscli` to the `dnf install` line in `jail.containerfile`.
- That's it. One small step, by design.

## How the iteration loop works (read me first)
See `objectives.md`. Short version:
1. Agent edits terraform / containerfile to gain a capability.
2. Agent commits + updates this file.
3. User applies terraform with their **admin** credentials, then rebuilds the container from `jail.containerfile`.
4. Repeat. The agent starts each iteration with no memory — this file IS its memory.

Key implication: the agent has no AWS/Cloudflare credentials inside the container yet. The user applies terraform externally with admin creds. So the agent can write terraform that creates credentials (e.g. an IAM access key) and templates them into `.env` via `local_sensitive_file` (`00_aws_users/env_file.tf` already does this pattern for Cloudflare tokens). It just can't *apply* that terraform itself until it has creds — chicken-and-egg, broken one step at a time.

## Current state of the jail
- Installed: bun, pi (+ pi-mcp-adapter), goose, opentofu 1.12.3, ripgrep, fd, git, **awscli (new)**.
- NOT installed: nothing else critical yet. No kubectl/helm (needed? maybe later for EKS chores).
- `.env` currently contains only: `CLOUDFLARE_API_KEY`, `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_GATEWAY_ID` (generated by `00_aws_users`).
- `~/.aws/credentials` does NOT exist in the container.
- MCP servers configured (`jail/mcp.json`): **opentofu** only (remote, `https://mcp.opentofu.org/mcp`).

## What "running the chores" actually requires
The chores (see `AGENTS.md`) boil down to, per folder:
- Check latest opentofu CLI version, latest fedora version, latest EKS version supported by AWS, latest provider versions.
- Bump `required_version` and provider versions in each `tofu.tf`.
- Run `tofu init -upgrade` + `tofu plan -lock=false` and report non-clean plans + deprecation warnings.

So the agent ultimately needs, inside the container:
1. **AWS credentials** so `tofu` can read state and the AWS provider can `plan`.
   - State backend is S3-on-Cloudflare-R2, using `profile = "cloudflare"` (see any `tofu.tf`). So `~/.aws/credentials` needs a `[cloudflare]` profile with **R2 S3-compatible access keys** (access_key_id + secret_access_key). NOTE: the Cloudflare terraform provider does NOT manage R2 S3 access keys — there's no such resource. These must be supplied by the user (e.g. as terraform variables, templated into a `local_sensitive_file` for `~/.aws/credentials`).
   - The AWS provider has no explicit profile, so it uses the default credential chain (env vars). A read-only IAM user's access key in `.env` (`AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`) would satisfy it. ReadOnlyAccess is enough for `plan`. `00_aws_users` is the natural place to add this IAM user + key + a `ReadOnlyAccess` attachment.
2. **GitHub MCP** for checking latest releases of software (opentofu, cilium, etc.). Not configured yet. Needs a GitHub token the user must provide (can't be bootstrapped by terraform without an existing token). Remote GitHub MCP is `https://api.githubcopilot.com/mcp/` with a bearer token; pi-mcp-adapter supports `"url"`, `"headers"`, and `"auth": "bearer"` / `"bearerTokenEnv"` (see `pi-mcp-adapter` README, "Server Options"). A stdio server via `bunx` is also an option since bun is installed.
3. **opentofu MCP** — already done.

## Suggested next steps (pick ONE per iteration, don't blow up scope)
- Next: add a read-only IAM user + access key in `00_aws_users`, template it into `.env` as `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`. This is the example given in `objectives.md`.
- After that: wire R2 state creds into `~/.aws/credentials` (user supplies via variables). Until this is done, `tofu init` will still fail on the backend even with AWS creds present.
- After that: add GitHub MCP + token.
- Only then can chores actually run end-to-end.

## Gotchas learned
- `tofu init` in `00_aws_users` currently fails: `failed to get shared config profile, cloudflare` — expected, no `~/.aws/credentials` in the container yet.
- The cloudflare provider (v5.21.0) has no resource for R2 S3-compatible access keys. Don't waste time looking for one.
- Chores must run in order, folder by folder, not in parallel. Always use `-lock=false` on `tofu plan`.
- `01_ipspace` and `05_cloudflare_tunnel` pin cloudflare provider to 5.20.0 while `00_aws_users` is on 5.21.0 — a chore will likely want to align these.

## Git
This iteration's commit installs awscli only. Nothing else changed.
