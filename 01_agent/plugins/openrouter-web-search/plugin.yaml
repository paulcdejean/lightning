name: openrouter-web-search
version: "1.0"
description: "Injects OpenRouter server tools (web_search, web_fetch, datetime) into the API request via LLM request middleware, and registers no-op handlers in the Hermes tool registry as a safety net."
