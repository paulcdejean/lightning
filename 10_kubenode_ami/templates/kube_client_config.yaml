---
apiVersion: v1
kind: Config
clusters:
  - name: kubernetes
    cluster:
      certificate-authority: /etc/lightning/pki_ca.crt
      server: __SERVER__
current-context: kubelet
contexts:
  - name: kubelet
    context:
      cluster: kubernetes
      user: kubelet
users:
  - name: kubelet
    user:
      exec:
        apiVersion: client.authentication.k8s.io/v1beta1
        command: aws
        env:
          - name: AWS_USE_DUALSTACK_ENDPOINT
            value: "true"
        args:
          - "eks"
          - "get-token"
          - "--cluster-name"
          - "__CLUSTER__"
          - "--region"
          - "__REGION__"
